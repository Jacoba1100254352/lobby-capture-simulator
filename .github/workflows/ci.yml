name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.12"
      - name: Check scripts
        run: make script-checks
      - name: Run tests
        run: make test
      - name: Install paper build dependencies
        timeout-minutes: 45
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          set -euxo pipefail
          sudo find /etc/apt/sources.list.d -type f \( -iname '*azure-cli*' -o -iname '*microsoft*' \) -print -delete
          sudo sed -i 's|http://azure.archive.ubuntu.com/ubuntu|https://archive.ubuntu.com/ubuntu|g' \
            /etc/apt/sources.list \
            /etc/apt/sources.list.d/*.list \
            /etc/apt/sources.list.d/*.sources \
            /etc/apt/apt-mirrors.txt 2>/dev/null || true
          sudo apt-get update -o Acquire::Retries=3 -o Acquire::http::Timeout=20 -o Acquire::https::Timeout=20
          sudo apt-get install -y -o Dpkg::Use-Pty=0 texlive-latex-recommended texlive-latex-extra texlive-fonts-recommended texlive-fonts-extra texlive-bibtex-extra texlive-pictures texlive-science inkscape poppler-utils zip unzip
      - name: Build and verify paper PDFs and submission package
        run: make paper-artifacts-check
        env:
          LOBBY_CAPTURE_REQUIRE_RELEASE_TAG: ${{ github.ref_type == 'tag' && startsWith(github.ref_name, 'paper-publication-readiness-') && '1' || '0' }}
      - name: Ensure deterministic generated artifacts are committed
        run: |
          git diff --exit-code -- \
            ':!paper/*.pdf' \
            ':!paper/figures/*.pdf' \
            ':!reports/paper-layout-audit.md' \
            ':!reports/manual-visual-audit.md'
